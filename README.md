## Basic API for link shortening

This is a simple API for shortening URLs. It allows users to create shortened links, retrieve original URLs, and manage their links.

Uses Java with Spring Boot framework.